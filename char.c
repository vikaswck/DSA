#include <stdio.h>
int main()
{
    int i;
    char ch; 
char str[20];
char st[20];
scanf("%s",&ch);
for(i=0;i<5;i++){
    scanf("%s",&st);
    scanf("%s", &str);
}
printf("%s", str);
printf("%s",st);
printf("%c",ch);
return 0;
}
